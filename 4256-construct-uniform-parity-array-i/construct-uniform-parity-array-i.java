class Solution {
    public boolean uniformArray(int[] nums1) {
        // logic : we have any two cases
        // 1. either all values are even or odd in that case we dont have do anything
        // 2. values are odd and even combined, then making odd from even and odd is very easy when i != j, 
        // so the answer is always be true.
        return true;
    }
}